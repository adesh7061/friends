<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="css/styles.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f4f4f4;
    }

    header {
      background-color: #3498db;
      color: #fff;
      padding: 10px;
      text-align: center;
    }

    main {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 80vh;
    }

    .notification-container {
      width: 300px;
      background-color: #fff;
      padding: 20px;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      margin-top: 20px;
      text-align: center;
    }

    .friend-request-item {
      margin-bottom: 15px;
    }

    .friend-request-buttons {
      display: flex;
      justify-content: space-around;
      margin-top: 10px;
    }

    .accept-button,
    .reject-button {
      padding: 8px 16px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 14px;
    }

    .accept-button {
      background-color: #2ecc71;
      color: #fff;
    }

    .reject-button {
      background-color: #e74c3c;
      color: #fff;
    }

    .btn-back {
      margin-top: 20px;
      background-color: #3498db;
      color: #fff;
      padding: 10px 20px;
      text-decoration: none;
      border-radius: 5px;
      cursor: pointer;
      font-size: 16px;
    }
  </style>
  <title>Friend Requests</title>
</head>
<body>

  <header>
    <h1>Friend Requests</h1>
  </header>

  <main>
    <div class="notification-container">
      <div class="friend-request-item">
        <% for(let i=0;i<notification.length;i++){ %>
            <p><strong><%=notification[i]%></strong> sent you a friend request.</p>
            <div class="friend-request-buttons">
               <form id="chi" method="POST">
                <% const accept = "A" + notification[i] %>
                <% const reject = "R" + notification[i] %>
                <button class="accept-button" formaction="/notification" name="foo" value="<%=accept%>">Accept</button>
                <button class="reject-button" formaction="/notification" name="foo" value="<%=reject%>">Reject</button>
            </form> 
            </div>
          </div>
                 
        <%} %>
      

      <!-- Add more friend request items as needed -->

      <a class="btn-back" href="/home" role="button">Back to Home</a>
    </div>
  </main>

</body>
</html>
